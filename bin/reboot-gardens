#!/bin/sh
#
# $Id: yoreboot.sh 204 2004-06-23 09:34:17Z mdb $
#
# Gracefully reboots all Game Gardens game servers running on this machine.

PIDS=`ps aux | grep ToyBoxServer | grep -v grep | awk '{ print $2 }' | sort -n`
if [ ! -z "$PIDS" ]; then
    echo $PIDS | xargs kill -INT
fi
